/* ============
 * Getters for the {{ name }} module
 * ============
 *
 * The getters that are available on the
 * {{ name }} module.
 */

const checkoutStatus = state => state.checkoutStatus;

export default {};
